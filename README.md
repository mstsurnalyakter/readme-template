# git state
   1. working directory/workspace(git init)
            |
 git status   git add .
            |
   2. staging area/index
            |
            git commit -m "message" git reset --soft HEAD^
            |
   3. local repository
   4. remote repository
